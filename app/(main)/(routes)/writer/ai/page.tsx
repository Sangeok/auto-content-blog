import AiWrite from "@/components/write/aiWrite";

export default function WriterAiPage() {
    // 어떤 글을 쓰게 만들지 input만들기
    // modal로 ai 응답을 받아오면 AiWrite에 전달하면서 AiWrite로 이동

    return (
        <AiWrite />
    );
}
